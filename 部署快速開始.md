# 部署快速開始指南

**版本**: 生產版本 v1.0  
**日期**: 2026-01-26

---

## 🚀 三種部署方式

### 方式一：Streamlit Cloud（最簡單，5分鐘）⭐ 推薦

#### 優點
- ✅ 免費
- ✅ 自動部署
- ✅ 自動 HTTPS
- ✅ 無需服務器

#### 步驟
1. **準備 GitHub 倉庫**
   ```bash
   git add .
   git commit -m "生產版本上線"
   git push origin main
   ```

2. **訪問 Streamlit Cloud**
   - 網址：https://share.streamlit.io
   - 使用 GitHub 帳號登錄

3. **創建應用**
   - 點擊 "New app"
   - 選擇您的倉庫
   - 主文件：`app.py`
   - 分支：`main`

4. **配置 Secrets**
   ```
   GEMINI_API_KEY = "your-api-key-here"
   ```

5. **部署**
   - 點擊 "Deploy"
   - 等待 1-2 分鐘
   - 獲得應用 URL

**完成！** 🎉

---

### 方式二：本地部署（適合測試）

#### 步驟
1. **安裝依賴**
   ```bash
   pip install -r requirements.txt
   ```

2. **配置 API Key**
   ```bash
   # 創建 .streamlit 目錄
   mkdir .streamlit
   
   # 創建 secrets.toml（Windows PowerShell）
   @"
   GEMINI_API_KEY = "your-api-key-here"
   "@ | Out-File -FilePath .streamlit\secrets.toml -Encoding utf8
   ```

3. **啟動**
   ```bash
   streamlit run app.py
   ```

4. **訪問**
   - 打開瀏覽器訪問：http://localhost:8501

---

### 方式三：Docker 部署（適合生產環境）

#### 步驟
1. **構建鏡像**
   ```bash
   docker build -t invoice-ocr-app .
   ```

2. **運行容器**
   ```bash
   docker run -d \
     -p 8501:8501 \
     -e GEMINI_API_KEY=your-api-key-here \
     -v $(pwd)/data:/app/data \
     --name invoice-ocr-app \
     invoice-ocr-app
   ```

3. **或使用 docker-compose**
   ```bash
   # 設置環境變數
   export GEMINI_API_KEY=your-api-key-here
   
   # 啟動
   docker-compose up -d
   ```

---

## ⚙️ 必需配置

### Gemini API Key
1. **獲取 API Key**
   - 訪問：https://makersuite.google.com/app/apikey
   - 登錄 Google 帳號
   - 創建新的 API Key
   - 複製 API Key

2. **配置方式**
   - **Streamlit Cloud**: 在 Secrets 中配置
   - **本地部署**: 在 `.streamlit/secrets.toml` 中配置
   - **Docker**: 使用環境變數 `-e GEMINI_API_KEY=...`

---

## ✅ 部署後驗證

### 必須測試
1. ✅ 訪問應用 URL
2. ✅ 註冊新帳號
3. ✅ 登錄功能
4. ✅ 上傳發票圖片測試 OCR
5. ✅ 測試數據編輯和刪除

### 建議測試
- 測試數據導入功能
- 測試數據導出功能（CSV/PDF）
- 測試圖表顯示
- 測試搜索和篩選功能

---

## 🆘 常見問題

### Q: 部署後無法訪問？
**A**: 檢查：
- 端口是否正確（8501）
- 防火牆設置
- Streamlit Cloud 部署狀態

### Q: OCR 識別失敗？
**A**: 檢查：
- API Key 是否正確配置
- API Key 是否有效
- 網絡連接是否正常

### Q: 無法登錄？
**A**: 檢查：
- 是否已註冊帳號
- 密碼是否正確
- 數據庫是否正常初始化

---

## 📞 需要幫助？

查看詳細文檔：
- `部署執行指南_20260126.md` - 詳細部署步驟
- `上線部署指南_20260126.md` - 完整部署指南
- `部署檢查清單_20260126.md` - 檢查清單

---

**準備好了嗎？開始部署吧！** 🚀
