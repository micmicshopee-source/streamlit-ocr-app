# 發票報帳系統 - Streamlit 優化設計方案

## 📋 設計概述

參考 Tailwind UI Detail Screens 的**布局理念和排版方式**，在保持 Streamlit 默認顏色和風格的前提下，優化發票報帳系統的界面排版和樣式，提升視覺層次和用戶體驗。

**設計原則：**
- ✅ 保持 Streamlit 默認色彩系統
- ✅ 保持 Streamlit 原生組件樣式
- ✅ 優化布局結構和排版
- ✅ 改進間距和視覺層次
- ✅ 增強卡片化設計（使用容器）

---

## 🎯 設計目標

1. **布局優化** - 採用垂直堆疊布局，信息流更清晰
2. **視覺層次** - 通過間距、容器、分組建立清晰層次
3. **卡片化設計** - 使用 `st.container()` 創建視覺分組
4. **響應式布局** - 利用 Streamlit 的列系統適配不同屏幕
5. **操作流程優化** - 上傳、識別、查看流程更順暢

---

## 🏗️ 整體布局架構

### 推薦方案：Stacked Layout（垂直堆疊布局）

```
┌─────────────────────────────────────────────────────────┐
│  Page Title (頁面標題)                                  │
│  📑 發票收據報帳小秘笈 Pro                                │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ┌──────────────────────────────────────────────────┐  │
│  │  Stats Section (統計指標區) - 4列並排            │  │
│  │  ┌────┐ ┌────┐ ┌────┐ ┌────┐                     │  │
│  │  │累計│ │累計│ │發票│ │缺失│                     │  │
│  │  │金額│ │稅額│ │總數│ │數據│                     │  │
│  │  └────┘ └────┘ └────┘ └────┘                     │  │
│  └──────────────────────────────────────────────────┘  │
│                                                         │
│  ┌──────────────────────────────────────────────────┐  │
│  │  Action Panel (操作面板) - 卡片容器                │  │
│  │  ┌──────────────────────────────────────────────┐  │  │
│  │  │ 📤 上傳辨識                                  │  │  │
│  │  │ [Tab: OCR識別 | 數據導入]                    │  │  │
│  │  │ [文件上傳區]                                 │  │  │
│  │  │ [開始辨識按鈕]                               │  │  │
│  │  └──────────────────────────────────────────────┘  │  │
│  └──────────────────────────────────────────────────┘  │
│                                                         │
│  ┌──────────────────────────────────────────────────┐  │
│  │  Charts Section (圖表展示區) - 2列並排            │  │
│  │  ┌──────────────┐ ┌──────────────┐              │  │
│  │  │ 會計科目分布  │ │ 每日支出趨勢  │              │  │
│  │  │ (圓餅圖)      │ │ (折線圖)      │              │  │
│  │  └──────────────┘ └──────────────┘              │  │
│  └──────────────────────────────────────────────────┘  │
│                                                         │
│  ┌──────────────────────────────────────────────────┐  │
│  │  Data Table Section (數據表格區)                  │  │
│  │  ┌──────────────────────────────────────────────┐  │  │
│  │  │ 篩選欄: [搜索框] [時間範圍] [導出按鈕]        │  │  │
│  │  ├──────────────────────────────────────────────┤  │  │
│  │  │ 數據表格 (可編輯)                              │  │  │
│  │  └──────────────────────────────────────────────┘  │  │
│  └──────────────────────────────────────────────────┘  │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

**布局特點：**
- 全寬垂直堆疊，信息流從上到下
- 每個功能區使用容器包裹，視覺分組清晰
- 統計指標置頂，數據一目了然
- 操作面板獨立區域，功能集中
- 圖表和表格分層展示，層次分明

---

## 📐 組件設計詳解

### 1. Page Title（頁面標題）

**設計：**
```python
st.title("📑 發票收據報帳小秘笈 Pro")
```

**優化建議：**
- 使用 `st.title()` 保持 Streamlit 默認樣式
- 可選：添加副標題 `st.caption()` 說明功能

---

### 2. Stats Section（統計指標區）

**布局：** 4列並排（響應式：大屏4列，中屏2列，小屏1列）

**實現方式：**
```python
# 使用容器包裹，增加視覺分組
with st.container():
    st.subheader("📊 統計報表")
    
    # 4列並排
    stat_col1, stat_col2, stat_col3, stat_col4 = st.columns(4)
    
    with stat_col1:
        st.metric("累計金額", f"${total_sum:,.0f}")
    with stat_col2:
        st.metric("累計稅額", f"${tax_sum:,.0f}")
    with stat_col3:
        st.metric("發票總數", f"{invoice_count} 筆")
    with stat_col4:
        st.metric("缺失數據", f"{missing_count} 筆", 
                 delta=f"-{invoice_count - missing_count} 正常" if invoice_count > 0 else None)
    
    st.divider()  # 分隔線
```

**優化要點：**
- 使用 `st.container()` 包裹整個統計區
- 使用 `st.subheader()` 添加區塊標題
- 使用 `st.divider()` 分隔不同區域
- 保持 `st.metric()` 默認樣式

---

### 3. Action Panel（操作面板）

**設計：** 獨立容器，卡片式視覺分組

**實現方式：**
```python
with st.container():
    st.subheader("📤 上傳辨識")
    
    # Tab 切換
    import_tab1, import_tab2 = st.tabs(["📷 OCR識別", "📥 數據導入"])
    
    with import_tab1:
        files = st.file_uploader(
            "批次選擇照片", 
            type=["jpg","png","jpeg"], 
            accept_multiple_files=True
        )
        
        if files:
            st.info(f"已選擇 {len(files)} 個文件")
        
        if files and st.button("開始辨識 🚀", type="primary", use_container_width=True):
            # 處理邏輯
            pass
    
    with import_tab2:
        # 數據導入邏輯
        pass
```

**優化要點：**
- 使用 `st.container()` 創建視覺分組
- 使用 `st.tabs()` 切換不同功能
- 使用 `st.info()` 提供狀態反饋
- 按鈕使用 `use_container_width=True` 全寬顯示

---

### 4. Charts Section（圖表展示區）

**布局：** 2列並排（響應式：小屏1列）

**實現方式：**
```python
with st.container():
    st.subheader("📈 數據分析")
    
    chart_col1, chart_col2 = st.columns(2)
    
    with chart_col1:
        st.markdown("**會計科目分布**")
        # Altair 圓餅圖
        st.altair_chart(chart, use_container_width=True)
    
    with chart_col2:
        st.markdown("**每日支出趨勢**")
        # Altair 折線圖
        st.altair_chart(line_chart, use_container_width=True)
    
    st.divider()
```

**優化要點：**
- 使用 `st.container()` 包裹圖表區
- 使用 `st.markdown("**標題**")` 添加圖表標題
- 圖表使用 `use_container_width=True` 自適應寬度
- 使用 `st.divider()` 分隔

---

### 5. Data Table Section（數據表格區）

**設計：** 獨立容器，包含篩選和表格

**實現方式：**
```python
with st.container():
    st.subheader("📋 數據稽核報表")
    
    # 篩選欄
    filter_col1, filter_col2, filter_col3 = st.columns([2, 1, 1])
    with filter_col1:
        search = st.text_input("🔍 關鍵字搜尋", placeholder="號碼/賣方/檔名...")
    with filter_col2:
        t_filter = st.selectbox("🕒 時間範圍", ["全部", "今天", "本週", "本月"])
    with filter_col3:
        st.write("")  # 空白，用於對齊
        st.write("")  # 空白，用於對齊
    
    # 數據表格
    if not df.empty:
        ed_df = st.data_editor(
            df, 
            use_container_width=True, 
            hide_index=True, 
            height=500,
            column_config=column_config
        )
        
        # 導出按鈕
        export_col1, export_col2 = st.columns(2)
        with export_col1:
            st.download_button("📥 導出 CSV", csv_data, "invoice_report.csv", use_container_width=True)
        with export_col2:
            st.download_button("📄 導出 PDF", pdf_data, "invoice_report.pdf", use_container_width=True)
    else:
        st.warning("⚠️ 目前無數據。請先嘗試上傳並辨識。")
```

**優化要點：**
- 使用 `st.container()` 包裹表格區
- 篩選欄使用列布局，對齊美觀
- 表格使用 `use_container_width=True` 全寬顯示
- 導出按鈕並排顯示，使用 `use_container_width=True`

---

## 🎨 Streamlit 樣式優化（不改變顏色）

### 1. 間距優化

**使用容器和分隔線：**
```python
# 每個功能區使用容器包裹
with st.container():
    # 內容
    pass

# 區域之間使用分隔線
st.divider()
```

### 2. 視覺分組

**使用 subheader 和 caption：**
```python
st.subheader("📊 統計報表")  # 區塊標題
st.caption("顯示所有發票的統計信息")  # 副標題說明
```

### 3. 按鈕優化

**使用 type 和 use_container_width：**
```python
# 主要操作按鈕
st.button("開始辨識 🚀", type="primary", use_container_width=True)

# 次要操作按鈕
st.button("取消", use_container_width=True)
```

### 4. 狀態反饋

**使用 info、success、warning、error：**
```python
st.info("💡 提示信息")
st.success("✅ 操作成功")
st.warning("⚠️ 警告信息")
st.error("❌ 錯誤信息")
```

---

## 📱 響應式布局

### 桌面端（≥1024px）
```python
# 4列統計卡片
stat_col1, stat_col2, stat_col3, stat_col4 = st.columns(4)

# 2列圖表
chart_col1, chart_col2 = st.columns(2)
```

### 平板端（768px - 1023px）
```python
# 2列統計卡片
stat_col1, stat_col2 = st.columns(2)
stat_col3, stat_col4 = st.columns(2)

# 1列圖表（堆疊）
st.altair_chart(chart1, use_container_width=True)
st.altair_chart(chart2, use_container_width=True)
```

### 移動端（<768px）
```python
# 1列統計卡片
st.metric("累計金額", f"${total_sum:,.0f}")
st.metric("累計稅額", f"${tax_sum:,.0f}")
# ...

# 1列圖表
st.altair_chart(chart, use_container_width=True)
```

**注意：** Streamlit 會自動處理響應式，無需額外 CSS

---

## 🚀 實施步驟

### Step 1: 重構布局結構

1. **移除左側上傳區的列布局**
   - 將上傳區移到主內容區
   - 使用垂直堆疊布局

2. **重組統計指標區**
   - 移到最頂部
   - 使用容器包裹
   - 4列並排顯示

3. **優化操作面板**
   - 獨立容器
   - Tab 切換保持不變
   - 添加視覺分組

4. **改進圖表展示**
   - 使用容器包裹
   - 添加圖表標題
   - 2列並排

5. **優化數據表格**
   - 獨立容器
   - 篩選欄優化布局
   - 導出按鈕並排

### Step 2: 添加視覺分組

- 每個功能區使用 `st.container()` 包裹
- 使用 `st.subheader()` 添加區塊標題
- 使用 `st.divider()` 分隔不同區域
- 使用 `st.caption()` 添加說明文字

### Step 3: 優化間距和對齊

- 篩選欄使用列布局對齊
- 按鈕使用 `use_container_width=True` 全寬
- 圖表使用 `use_container_width=True` 自適應

---

## 📊 布局對比

### 當前布局 vs 優化後布局

| 項目 | 當前布局 | 優化後布局 |
|------|---------|-----------|
| **整體結構** | 左側上傳 + 右側內容（2列） | 垂直堆疊，全寬展示 |
| **統計指標** | 在右側主內容區頂部 | 移到最頂部，獨立容器 |
| **上傳區** | 左側固定列 | 移到主內容區，獨立容器 |
| **圖表** | 在統計下方 | 獨立容器，視覺分組清晰 |
| **表格** | 在圖表下方 | 獨立容器，篩選欄優化 |
| **視覺分組** | 較弱 | 使用容器和分隔線，分組清晰 |
| **間距** | 默認間距 | 通過容器和分隔線優化 |

---

## 💡 實現示例

### 完整布局結構

```python
# 1. 頁面標題
st.title("📑 發票收據報帳小秘笈 Pro")

# 2. 統計指標區
with st.container():
    st.subheader("📊 統計報表")
    stat_col1, stat_col2, stat_col3, stat_col4 = st.columns(4)
    # ... 統計指標
    st.divider()

# 3. 操作面板
with st.container():
    st.subheader("📤 上傳辨識")
    # ... 上傳邏輯
    st.divider()

# 4. 圖表展示區
with st.container():
    st.subheader("📈 數據分析")
    chart_col1, chart_col2 = st.columns(2)
    # ... 圖表
    st.divider()

# 5. 數據表格區
with st.container():
    st.subheader("📋 數據稽核報表")
    # ... 篩選和表格
```

---

## ✅ 優化檢查清單

- [x] 保持 Streamlit 默認顏色和風格
- [x] 使用垂直堆疊布局
- [x] 每個功能區使用容器包裹
- [x] 添加區塊標題和分隔線
- [x] 統計指標置頂，4列並排
- [x] 操作面板獨立區域
- [x] 圖表區獨立容器，2列並排
- [x] 表格區獨立容器，篩選欄優化
- [x] 按鈕使用全寬顯示
- [x] 圖表使用自適應寬度
- [x] 響應式布局考慮

---

## 📝 總結

本設計方案在**保持 Streamlit 默認顏色和風格**的前提下，通過以下方式優化界面：

**核心優化點：**
1. **布局重構** - 從左右分欄改為垂直堆疊，信息流更自然
2. **視覺分組** - 使用容器、標題、分隔線建立清晰層次
3. **統計指標置頂** - 數據一目了然，成為視覺焦點
4. **操作面板獨立** - 功能集中，流程更順暢
5. **圖表和表格分層** - 每個區域獨立容器，層次分明

**技術實現：**
- 使用 `st.container()` 創建視覺分組
- 使用 `st.subheader()` 和 `st.divider()` 建立層次
- 使用列布局實現響應式設計
- 保持所有 Streamlit 原生組件樣式

**下一步行動：**
1. 確認設計方案
2. 開始實施布局重構
3. 測試響應式效果

---

*設計方案版本：v2.0 (Streamlit 優化版)*  
*設計日期：2026-01-26*  
*設計原則：保持 Streamlit 默認樣式，優化布局和排版*
